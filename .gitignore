# dependencies
node_modules
.pnp
.pnp.js

# build / deploy artifacts
dist
dist-ssr
*.local
*.zip

# env — NUNCA commitear claves
.env
.env.*
!.env.example

# MCP local: contiene el token de Netlify, NO commitear
.mcp.json

# Estado local del CLI de Netlify (deploys), no compartir
.netlify

# Claude Code: ajustes locales personales (no compartir)
.claude/settings.local.json

# Logo original sin optimizar (1.1MB). La app usa public/logo-mark.png; el
# original se mantiene en disco pero no se versiona para no inflar el repo.
public/logo.png

# supabase
.branches
.temp
supabase/.temp

# editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# tests
coverage
